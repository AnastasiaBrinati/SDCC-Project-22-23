syntax = "proto3";

package proto;

service Searcher {
    rpc Search(SearchRequest) returns (SearchReply) {}
}

message SearchRequest{
    string city = 1;
}

message SearchReply{
    bool correct = 1;
    string city = 2;
    float temperature = 3;
    float humidity = 4;
    string cloudiness = 5;
}