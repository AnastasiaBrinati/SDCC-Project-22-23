syntax = "proto3";

package proto;

service Loginner {
    rpc Login(LoginRequest) returns (LoginReply) {}
}

message LoginRequest{
    string username = 1;
    string password = 2;
}

message LoginReply{
    bool correct = 1;
}