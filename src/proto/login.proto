syntax = "proto3";

package proto;

service Loginner {
    rpc SayHello(HelloRequest) returns (HelloReply) {}
}

message HelloRequest{
    string name = 1;
}

message HelloReply{
    string msg = 1;
}